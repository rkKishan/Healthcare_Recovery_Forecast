# Web layer
Flask==3.1.3
Flask-Cors==6.0.5
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
SQLAlchemy==2.0.52
gunicorn==23.0.0

# Auth
PyJWT==2.13.0
bcrypt==5.0.0
Flask-Limiter==4.0.0
# Verifies Google Sign-In ID tokens against Google's public keys.
# The [requests] extra is load-bearing: google.auth.transport.requests is what
# fetches Google's signing keys, and it needs `requests`, which google-auth
# does not depend on otherwise. Without the extra the import fails at runtime
# and every sign-in returns 503 -- which only showed up in CI, because a
# leftover streamlit install was supplying `requests` locally.
google-auth[requests]==2.57.1

# Config
python-dotenv==1.2.3

# Data & ML
pandas==3.0.5
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.2.0
shap==0.51.0
joblib==1.6.0

# Excel upload support
openpyxl==3.1.5

# PDF reports (matplotlib renders the charts, reportlab lays out the pages)
reportlab==5.0.1
matplotlib==3.11.1

# MySQL driver — only needed when DATABASE_URL points at MySQL
PyMySQL==1.2.0

# Postgres driver, for a managed database such as Neon or Render Postgres.
# DATABASE_URL then looks like postgresql+psycopg://user:pass@host/db
psycopg[binary]==3.2.12
groq>=1.7,<2
